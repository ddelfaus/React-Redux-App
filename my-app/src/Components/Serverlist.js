import React from 'react';
import { connect} from 'react-redux'

const Serverlist = (props) => {
    return (
        <div>

        </div>
    )
}
const mapStateToProps = state => {
    return(
        <div>
            
        </div>
    )


}

export default connect(mapSateToProps, [])(Serverlist);